const read = require('readline-sync');

function aumentarEstima(nome, quantidade){
    for(let i = 0; i < lerQuantidade; i++){
        console.log(lerNome + " é a pessoa mais inteligente que já passou por aqui");
    }
}

let lerNome = read.question("Qual o seu nome?");
let lerQuantidade = parseInt(read.question("Você precisa aumentar sua autoestima quantas vezes?"));

aumentarEstima(lerNome, lerQuantidade);



